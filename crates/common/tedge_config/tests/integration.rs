use test_utils::tedge;

/// testing changing the log path will create log file in the new path

#[test]
fn it_works() {
    //let mut result = tedge!(config set "tmp.path", "/tmp" --config-dir "/path");
    //println!("{:#?}", result.output());
}
